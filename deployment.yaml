apiVersion: apps/v1
kind: Deployment
metadata:
  name: abc-deployment
spec:
  selector:
    matchLabels:
      app: abc
  replicas: 5
  template:
    metadata:
      labels:
        app: abc
    spec:
      containers:
        - name: abc
          image: abc:latest
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8088